import { MouseOnMaskDirective } from './mouse-on-mask.directive';

describe('MouseOnMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseOnMaskDirective();
    expect(directive).toBeTruthy();
  });
});
